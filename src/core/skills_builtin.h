// SKILL_ENGINE -- built-in example skills (seeded next to the exe).
#pragma once

#include <string>

// Seed built-in skills (skill_maker, ...) under <skillsDir>/ if missing.
void seedBuiltinSkills(const std::string& skillsDir);
