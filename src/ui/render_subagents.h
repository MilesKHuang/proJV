#pragma once
#include <vector>
#include "core/sub_agent_manager.h"

// Render the SubAgents panel inline (embedded in the right panel below TODO).
void renderSubAgentPanelInline(SubAgentManager* mgr);
