{
  "name": "monica",
  "description": "Supervisor-driven multi-agent project tidy-up: format files, strip full-width chars, translate Chinese comments to English, scan for dead code (report only).",
  "tools": ["read_file", "write_file", "edit_file", "grep_files", "file_search", "exec_shell"],
  "tool_mode": "auto",
  "dynamic_dispatch": true,
  "dispatch_tool": "emit",
  "max_tool_iters": 60,
  "max_recursion": 3,
  "max_rounds": 12,
  "converge": "none",
  "stop_when": {"slot": "supervisor", "equals": "DONE"},
  "agents": [
    {"id": "supervisor", "display_name": "Supervisor",     "prompt": "supervisor.md"},
    {"id": "formatter",  "display_name": "Formatter",      "prompt": "formatter.md"},
    {"id": "translator", "display_name": "Translator",     "prompt": "translator.md"},
    {"id": "auditor",    "display_name": "DeadCode Auditor","prompt": "auditor.md"}
  ],
  "round_steps": [
    {"actions": [
      {"agent": "supervisor", "tool": "emit", "capture": "summary", "into": "supervisor", "emit": "message",
       "message": "Project root: {{topic}}\n\nYou are MONICA, the supervisor. Each round: (1) explore the project, (2) pick the next small batch and dispatch via request_agent to formatter / translator / auditor, (3) collect reports. When everything is processed, reply with summary exactly DONE.\n\n(round {{round}}/{{max_rounds}})"}
    ]}
  ],
  "on_converge": []
}
